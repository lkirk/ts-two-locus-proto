name: build and test c
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: mamba-org/setup-micromamba@v1
      with:
        micromamba-version: '1.3.1-0'
        environment-file: dev-conda-env.yml
        environment-name: test
        init-shell: >-
          bash
        cache-environment: true
    - name: build and test
      working-directory: c
      shell: bash -el {0}
      run: |
        python -c 'import tskit; print(tskit.__version__)'
        # cat ~/.bashrc
        # cat /home/runner/micromamba/etc/profile.d/micromamba.sh
        # meson setup build
        # ninja -C build test
